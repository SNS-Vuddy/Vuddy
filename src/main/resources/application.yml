
server:
  port: 8080
  context-path: /myapp2

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vuddy
    username: root
    password: abcd1234
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: location
    producer:
      bootstrap-servers: localhost:9092
  redis:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 2
    port: 6379
    host: 127.0.0.1